#include <stdio.h>

int main()

    {
        int a=10, b=-5;

        if(a>0 && b>0) 
        {
            printf("Ambos os números são positivos\n");
        } else {
            printf("Ao menos um dos números é negativo\n");
        }

        return 0;

}